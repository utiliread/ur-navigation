{
  "name": "ur-navigation",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/**/*.{css,html} dist",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "copyfiles": "^1.2.0",
    "rimraf": "^2.6.2",
    "typescript": "^2.5.3"
  },
  "dependencies": {
    "aurelia-framework": "^1.1.5",
    "aurelia-router": "^1.4.0",
    "ur-route-mapper": "^1.0.0"
  }
}
